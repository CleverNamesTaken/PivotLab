[Unit]
Description=CallMe Flask Application
After=network.target

[Service]
User=root
WorkingDirectory=/opt/callme
ExecStart=flask run --host 0.0.0.0 --port 80
Restart=always

[Install]
WantedBy=multi-user.target

